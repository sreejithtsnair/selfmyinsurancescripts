#Script in Python to verify that app web interface is up and running/validates the html is generated from flask,requests,scape

app = flask(__name__)
@app.route('/')
def hello():
	return 'Hello,World'
@app.route('/greet')
	name = request.args['name']
	return ''
	'head'
	htm
".format(escape('name'))